const mapText = document.querySelector('.waypoint-pin-view__text');
console.log(mapText)
document.addEventListener('DOMContentLoaded', () => {
    console.log(mapText)
})

setInterval(() => {
    const mapText = document.querySelector('.waypoint-pin-view__word');
    console.log(mapText)
}, 1000)