class Swiper {
    constructor(swiperBlock, params) {
        this.baseBlock = swiperBlock;
        this.params = params;
        this.init();
    }

    init() {
        const wrapper = this.baseBlock.querySelector('.swiper-wrapper');
        
    }
}